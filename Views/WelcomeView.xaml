<UserControl x:Class="S3Eksamen.Views.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:S3Eksamen.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{StaticResource sharedLoginViewmodel}">
    <Canvas Background="White" Visibility="{Binding WelcomeVisibility.Visibility}">
        <TextBlock FontSize="50" Text="{Binding User.UserName}" Canvas.Left="342" Canvas.Top="192"/>
    </Canvas>
</UserControl>
